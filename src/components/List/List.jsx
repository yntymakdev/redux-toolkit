import React from "react";
// import Menu from "../Menu/Menu";

const List = () => {
  // const todo = useSelector((state) => state.todo.todo);
  return (
    <div>
      {/* {todo.map((el, index) => (
        <div key={index}>
          <h1>{el.name}</h1>
        </div>
      ))} */}
    </div>
    // <Menu el={el} key={index} />
  );
};

export default List;
